@model ASP_P22.Data.Entities.Product

<div class="col">
    <div class="card mx-3 h-100">
        <a asp-controller="Shop" asp-action="Product" asp-route-id="@(Model.Slug ?? Model.Id.ToString())">
            <img src="@("/Storage/Item/" + (Model.ImagesCsv?.Split(',').First() ?? "no-image.jpg") )" class="card-img-top" alt="Photo">
        </a>
        <div class="card-body">
            <div data-cart-product="@Model.Id" class="card-fab"><i class="bi bi-cart-plus"></i></div>

            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text">@Model.Description</p>
        </div>
        <div class="card-footer">
            <strong>₴ @Model.Price</strong>, у наявності - @Model.Stock
            <i class="bi bi-star"></i> @(Model.Rates.Where(r => (r.Rating ?? 0) > 0).Average(r => r.Rating))
        </div>        
    </div>
</div>
@*
    Д.З. Обмежити висоту блоку з коментарями на сторінці товару. Додати скролінг.
    У профілі користувача приховати блок з покупками (історією) при перегляді
    іншими користувачами / анонімному перегляді.
    ---
    Встановити Postman https://www.postman.com/downloads/
*@